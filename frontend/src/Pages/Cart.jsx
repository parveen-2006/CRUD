import React, { useState } from "react";

export default function Cart() {
  const [cart, setCart] = useState([]);
  return <>
    helllo guys here only those items will be shown which is selected by a particualr user.
  </>;
}
